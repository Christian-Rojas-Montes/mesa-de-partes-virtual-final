{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm install --include=dev --cache /tmp/npm-cache && npm run build"
  },
  "deploy": {
    "preDeployCommand": [
      "php artisan migrate --force && if [ \"$RUN_DATABASE_SEEDERS\" = \"true\" ]; then php artisan db:seed --force; fi"
    ],
    "startCommand": "/start-container.sh",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
